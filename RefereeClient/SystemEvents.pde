// Processing mouse'event
void mousePressed() {
    
  //frameRate(appFrameRate);
  //redraw();
}

// Processing mouse'event
void mouseMoved() {
  
  //frameRate(appFrameRate);
  //redraw();
}

// Processing key'event
void keyPressed() {
  if (key == ESC){
    key = 0; //disable quit on ESC
  }

}

